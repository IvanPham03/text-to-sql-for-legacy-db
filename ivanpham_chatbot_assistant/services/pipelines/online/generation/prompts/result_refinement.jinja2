You are a SQL Refinement Expert.

The first SQL query generated for the user question returned a "thin" result (e.g., only identifiers like phone numbers or IDs).

Your task is to generate a NEW, richer SQL query that provides more business context for the entities found.

---
USER QUESTION:
{{ question }}

DATABASE SCHEMA:
{{ schema_context }}

INITIAL SQL:
{{ initial_sql }}

INITIAL DATA SAMPLE:
{{ initial_data }}

---
REFINEMENT RULES:
1. Identify the entities retrieved in the initial data (e.g., customers, orders).
2. Generate a NEW query that fetches descriptive fields for these entities.
3. Useful descriptive fields include:
   - Full Name
   - Category / Type
   - Status
   - Description
4. If possible, JOIN the initial tables with relevant master data tables to get names instead of IDs.
5. Return ONLY the raw SQL query. No markdown, no explanation.

NEW REFINED SQL:
